-- =====================================================================
-- Тема урока — свободный текст, который вводит преподаватель при
-- завершении урока. Раньше тема выбиралась из справочника topics/modules,
-- но этот справочник никогда не заполнялся (0 строк в проде) — админ-панели
-- для его наполнения тоже нет, так что выбор темы был фактически мёртвой
-- функцией. Заменяем на простое текстовое поле на самом уроке.
-- =====================================================================

alter table lessons add column if not exists topic_text text;
