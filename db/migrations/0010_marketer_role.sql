-- =====================================================================
-- Новая роль сотрудника "marketer" — видит только Календарь и Задачи.
-- Отдельная миграция: добавленное значение enum нельзя использовать
-- в той же транзакции, где оно создано (в т.ч. в CHECK-constraint'ах).
-- =====================================================================

alter type user_role add value if not exists 'marketer';
