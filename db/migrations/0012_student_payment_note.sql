-- =====================================================================
-- Заметка администратора «когда была последняя оплата» — отдельно от
-- реальной истории в payments (v_student_payment_status), просто пометка
-- для себя, необязательная и свободно редактируемая.
-- =====================================================================

alter table students add column if not exists payment_note_at date;
