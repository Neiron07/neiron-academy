-- =====================================================================
-- Плановая дата следующей оплаты — необязательное поле, которое админ
-- выставляет вручную "для себя", в отличие от next_payment_estimate
-- (расчёт по темпу занятий в estimateNextPayment).
-- =====================================================================

alter table students add column if not exists next_payment_at date;
